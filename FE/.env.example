# Backend API - phải trùng PORT với BE (xem BE .env hoặc lệnh "Server running on port ...")
# Để trống = dùng mock (không gọi BE). Nhớ restart FE sau khi sửa file này.
VITE_API_URL=http://localhost:6969
